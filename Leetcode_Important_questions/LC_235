/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */
//soluton 01
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */

class Solution {
    TreeNode ans = null;
    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
        if(p.val<q.val){
            getLCA(root,p,q);
        }else{
            getLCA(root,q,p);
        }
        return ans;
    }

    public void getLCA(TreeNode root, TreeNode p, TreeNode q){
        if(root==null)return;
        if(root.val==p.val || root.val==q.val){ans = root;}
        else if(root.val>p.val && root.val<q.val){ans = root; return;}
        else if(root.val>p.val && root.val>q.val){
             lowestCommonAncestor(root.left,p,q);
        }else{
            lowestCommonAncestor(root.right,p,q);
        }
    }
}


//solution 02
class Solution {
    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {
        ArrayList<TreeNode> path1 = new ArrayList<>();
        ArrayList<TreeNode> path2 = new ArrayList<>();
        makePath(p,root,path1);
        makePath(q,root,path2);
        int ans = 0;

        for(int i = 0;i<path1.size() && i<path2.size();i++){
            if(path1.get(i).val!=path2.get(i).val){
                return path2.get(i-1);
            }
            ans = i;
        }
        return path1.get(ans);
    }

    public void makePath(TreeNode p,TreeNode root,ArrayList<TreeNode> path){
        path.add(root);
        if(root.val==p.val)return;
        if(root.val<p.val){
            makePath(p,root.right,path);
        }else{
            makePath(p,root.left,path);
        }
    }
}